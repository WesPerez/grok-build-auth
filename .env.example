# Required — YesCaptcha (Cloudflare Turnstile solver)
YESCAPTCHA_API_KEY=

# Required when using default email backend (-e tempmail)
TEMPMAIL_API_KEY=

# Optional — Cloudflare D1 alias mailbox (-e cloudflare)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DB_ID=
# Comma-separated domains you control (must match your D1 mail schema)
ALIAS_MAIL_DOMAINS=mail.example.com
# ALIAS_EXTRA_DOMAINS=

# Server auth directory. Keep it outside the Git checkout (0700; files 0600).
CLIPROXYAPI_AUTH_DIR=/var/lib/grok-build-auth/cliproxyapi-auth

# Optional — self-hosted IMAP mailbox (-e imap)
# Point to your own mail server (e.g. Mailu/Dovecot)
IMAP_SERVER=<imap-host>
IMAP_USERNAME=<mailbox@example.com>
IMAP_PASSWORD=
IMAP_EMAIL=<mailbox@example.com>
# IMAP_PORT=143
# IMAP_SSL=true

# Optional HTTP(S) proxy
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
